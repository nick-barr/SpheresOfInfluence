function Spheres() {

    // circle.beginPath();
    // circle.arc(x, y, 5, 0, 2*Math.PI);
    // circle.fillStyle = "green";
    // circle.fill();
    // circle.stroke;

    function createRandomNum() {
        return Math.ceil(Math.random() * 299) * (Math.round(Math.random()) ? 1 : -1) / 100
    };
}
